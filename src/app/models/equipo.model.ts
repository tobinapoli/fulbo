export interface Equipo {
  id: number; // O podría ser string si prefieres IDs no numéricos
  nombre: string;
  // Aquí podrías añadir más propiedades en el futuro, como:
  // escudoUrl?: string;
  // ciudad?: string;
}
